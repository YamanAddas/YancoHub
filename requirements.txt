Flask==3.1.3
pywebview==5.3.2
requests>=2.31.0
psutil>=5.9.0
hidapi>=0.14.0
pystray>=0.19.5
Pillow>=10.0.0
vdf>=3.4
# Optional: legendary-gl — fallback for Epic if catcache.bin is unavailable
# pip install legendary-gl && legendary auth
